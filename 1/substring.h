#ifndef SUBSTRING_H
#define SUBSTRING_H

//find the beginning of a substring in a string
int substring_position(const char *needle, const char *hay);

//check if a string is the prefix of another
bool is_prefix(const char *needle, const char *hay);

#endif
